# JobMato ChatBot Configuration
# This file contains environment variables that can override settings in config.py
# Most configuration is now centralized in config.py for better organization

# Flask Environment
FLASK_ENV=development
FLASK_DEBUG=True

# Google Gemini API (Required for LLM functionality)
GEMINI_API_KEY=your_gemini_api_key_here

# JobMato API Configuration
JOBMATO_API_BASE_URL=https://backend-v1.jobmato.com

# MongoDB Configuration (Optional - for chat persistence)
MONGODB_URI=mongodb://localhost:27017/

# Redis Configuration (Optional - for WebSocket session management)
# For local Redis:
REDIS_URL=redis://localhost:6379
REDIS_SSL=False

# For online Redis services (recommended for production):
# REDIS_URL=redis://username:password@host:port
# REDIS_SSL=True

# JWT Configuration (Optional - for enhanced security)
JWT_SECRET=your_jwt_secret_here

# Server Configuration (Optional - defaults in config.py)
PORT=5003
HOST=0.0.0.0

# Logging Configuration (Optional)
LOG_LEVEL=INFO

# Session Configuration (Optional - defaults in config.py)
SESSION_TIMEOUT_HOURS=24
MAX_CONVERSATION_HISTORY=10

# Rate Limiting (Optional - defaults in config.py)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# WebSocket Configuration (Optional - defaults in config.py)
SOCKETIO_CONNECT_TIMEOUT=60000
SOCKETIO_PING_TIMEOUT=60000
SOCKETIO_PING_INTERVAL=25000

# Online Redis Services (for reference - no environment variables needed)
# These services are configured in config.py and can be used by setting REDIS_URL:
# - Redis Cloud: https://redis.com/try-free/
# - Upstash: https://upstash.com/
# - Railway: https://railway.app/
# - Render: https://render.com/ 